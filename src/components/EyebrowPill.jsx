export default function EyebrowPill({ children }) {
  return (
    <span className="inline-block rounded-full bg-[#FBF1E6] px-[14px] py-[6px] text-center text-[11px] font-bold uppercase tracking-[0.06em] text-[#7C3AED]">
      {children}
    </span>
  );
}
